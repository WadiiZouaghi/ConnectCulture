{% extends 'base.html.twig' %}

{% block title %}Create New Group{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create New Group</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Create Group</button>  <!-- Added submit button -->
        {{ form_end(form) }}
    </div>
{% endblock %}
