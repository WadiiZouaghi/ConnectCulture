{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Group</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Update Group</button>
    {{ form_end(form) }}
    <a href="{{ path('group_list') }}" class="btn btn-secondary">Back</a>
{% endblock %}