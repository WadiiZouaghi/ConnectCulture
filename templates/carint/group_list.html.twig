<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="keywords" content="Culture Connect, Events, Cultural Events, Event Groups" />
    <meta name="description" content="Browse and join cultural event groups on Culture Connect to explore vibrant experiences worldwide." />
    <meta name="author" content="" />
    <link rel="shortcut icon" href="{{ asset('images/fevicon.png') }}" type="image/png">

    <title>Culture Connect - Event Groups</title>

    <link rel="stylesheet" type="text/css" href="{{ asset('css/bootstrap.css') }}" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="{{ asset('https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css') }}" />
    <link href="{{ asset('css/font-awesome.min.css') }}" rel="stylesheet" />
    <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet" />
</head>

<body class="sub_page">
    <!-- Header Section -->
    <div class="hero_area">
        <header class="header_section">
            <div class="header_top">
                <div class="container-fluid">
                    <div class="contact_nav">
                        <a href="" class="contact-link">
                            <i class="fa fa-phone" aria-hidden="true"></i>
                            <span>Call: +01 123455678990</span>
                        </a>
                        <a href="" class="contact-link">
                            <i class="fa fa-envelope" aria-hidden="true"></i>
                            <span>Email: support@cultureconnect.com</span>
                        </a>
                        <a href="" class="contact-link">
                            <i class="fa fa-map-marker" aria-hidden="true"></i>
                            <span>Global Events</span>
                        </a>
                    </div>
                </div>
            </div>
            <div class="header_bottom">
                <div class="container-fluid">
                    <nav class="navbar navbar-expand-lg custom_nav-container">
                        <a class="navbar-brand" href="{{ path('group_list') }}">
                            <span>Culture Connect</span>
                        </a>
                        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                            <span class=""></span>
                        </button>
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <ul class="navbar-nav">
                                <li class="nav-item active">
                                    <a class="nav-link" href="{{ path('group_list') }}">Events <span class="sr-only">(current)</span></a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">Services</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">About</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="#">Contact Us</a>
                                </li>
                                <form class="form-inline">
                                    <button class="btn my-2 my-sm-0 nav_search-btn" type="submit" aria-label="Search">
                                        <i class="fa fa-search" aria-hidden="true"></i>
                                    </button>
                                </form>
                            </ul>
                        </div>
                    </nav>
                </div>
            </div>
        </header>
    </div>

    <!-- Main Content Section -->
    <section class="event-groups-section layout_padding-top">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="heading_container heading_center">
                        <h2>Event Groups</h2>
                    </div>
                    {% for message in app.flashes('success') %}
                        <div class="alert alert-success">{{ message }}</div>
                    {% endfor %}
                    <form method="GET" action="{{ path('group_list') }}" class="search-form mb-4">
                        <div class="search-container d-flex flex-row justify-content-center align-items-center">
                            <div class="search-input-group d-flex align-items-center">
                                <input type="text" name="q" value="{{ searchQuery|default('') }}" class="form-control search-input" placeholder="Search groups by name, description, or location" id="searchInput">
                                <button type="submit" class="search-button">
                                    <i class="fa fa-search" aria-hidden="true"></i>
                                </button>
                            </div>
                            <div class="search-filters d-flex align-items-center ml-3">
                                <select name="location" class="form-control filter-select mr-2">
                                    <option value="">All Locations</option>
                                    {% for loc in locations %}
                                        <option value="{{ loc.location }}" {% if location == loc.location %}selected{% endif %}>{{ loc.location }}</option>
                                    {% endfor %}
                                </select>
                                <select name="visibility" class="form-control filter-select mr-2">
                                    <option value="">All Visibilities</option>
                                    {% for vis in visibilities %}
                                        <option value="{{ vis.visibility }}" {% if visibility == vis.visibility %}selected{% endif %}>{{ vis.visibility|capitalize }}</option>
                                    {% endfor %}
                                </select>
                                <input type="date" name="date" class="form-control filter-date mr-2" value="{{ date|default('') }}">
                                <button type="button" class="btn btn-link clear-filters">Clear Filters</button>
                            </div>
                            <div id="suggestions" class="suggestions-list" style="display: none;"></div>
                        </div>
                    </form>
                    <div class="text-center mb-4">
                        <a href="{{ path('group_create') }}" class="create-event-btn">Create New Event</a>
                    </div>
                    <div class="row" id="eventGroups">
                        {% for group in groups %}
                            <div class="col-12 mb-4 event-group" data-location="{{ group.location|lower }}">
                                <div class="group-card">
                                    <div class="group-content">
                                        <h3 class="group-name">{{ group.name }}</h3>
                                        <p class="group-details">{{ group.description|length > 100 ? group.description|slice(0, 100) ~ '...' : group.description|default('No description provided') }}</p>
                                        <p class="group-meta">Location: {{ group.location|default('Not specified') }} | Date: {{ group.eventDate ? group.eventDate|date('Y-m-d H:i') : 'TBD' }} | Max: {{ group.maxParticipants|default('Unlimited') }} | {{ group.visibility|capitalize }}</p>
                                    </div>
                                    <div class="group-actions d-flex justify-content-end">
                                        <a href="{{ path('group_view', {'id': group.id}) }}" class="action-btn btn mr-2 view-btn">View</a>
                                        <a href="{{ path('group_edit', {'id': group.id}) }}" class="action-btn btn mr-2 edit-btn">Edit</a>
                                        <form action="{{ path('group_delete', {'id': group.id}) }}" method="post" style="display:inline;">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
                                            <button type="submit" class="action-btn btn delete-btn" onclick="return confirm('Are you sure?')">Delete</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        {% else %}
                            <div class="col-12">
                                <div class="no-groups">No event groups found</div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Info Section -->
    <section class="info_section layout_padding2">
        <div class="container">
            <div class="row">
                <div class="col-md-6 col-lg-3 info_col">
                    <div class="info_contact">
                        <h4>Contact Us</h4>
                        <div class="contact_link_box">
                            <a href="">
                                <i class="fa fa-map-marker" aria-hidden="true"></i>
                                <span>Global Events</span>
                            </a>
                            <a href="">
                                <i class="fa fa-phone" aria-hidden="true"></i>
                                <span>Call: +01 1234567890</span>
                            </a>
                            <a href="">
                                <i class="fa fa-envelope" aria-hidden="true"></i>
                                <span>support@cultureconnect.com</span>
                            </a>
                        </div>
                    </div>
                    <div class="info_social">
                        <a href=""><i class="fa fa-facebook" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-twitter" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-linkedin" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-instagram" aria-hidden="true"></i></a>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3 info_col">
                    <div class="info_detail">
                        <h4>About Culture Connect</h4>
                        <p>Connecting people through cultural events worldwide.</p>
                    </div>
                </div>
                <div class="col-md-6 col-lg-2 mx-auto info_col">
                    <div class="info_link_box">
                        <h4>Links</h4>
                        <div class="info_links">
                            <a class="active" href="{{ path('group_list') }}">
                                <img src="{{ asset('images/nav-bullet.png') }}" alt=""> Events
                            </a>
                            <a href="">
                                <img src="{{ asset('images/nav-bullet.png') }}" alt=""> About
                            </a>
                            <a href="">
                                <img src="{{ asset('images/nav-bullet.png') }}" alt=""> Services
                            </a>
                            <a href="">
                                <img src="{{ asset('images/nav-bullet.png') }}" alt=""> Contact Us
                            </a>
                        </div>
                    </div>
                </div>
                <div class="col-md-6 col-lg-3 info_col">
                    <h4>Subscribe</h4>
                    <form action="#">
                        <input type="text" placeholder="Enter email" class="form-control" required />
                        <button type="submit" class="btn btn-primary">Subscribe</button>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer Section -->
    <footer class="footer_section">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center">
                    <p>
                        © {{ "now"|date("Y") }} All Rights Reserved By
                        <a href="https://cultureconnect.com/">Culture Connect</a>
                    </p>
                    <div class="footer-links">
                        <a href="">Privacy Policy</a> | <a href="">Terms of Service</a>
                    </div>
                    <div class="footer-social">
                        <a href=""><i class="fa fa-facebook" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-twitter" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-linkedin" aria-hidden="true"></i></a>
                        <a href=""><i class="fa fa-instagram" aria-hidden="true"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script type="text/javascript" src="{{ asset('js/jquery-3.4.1.min.js') }}" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous" defer></script>
    <script type="text/javascript" src="{{ asset('js/bootstrap.js') }}" defer></script>
    <script type="text/javascript" src="{{ asset('https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js') }}" defer></script>
    <script type="text/javascript" src="{{ asset('js/custom.js') }}" defer></script>

    <script>
        document.querySelector('.clear-filters').addEventListener('click', function() {
            document.querySelector('select[name="location"]').value = '';
            document.querySelector('select[name="visibility"]').value = '';
            document.querySelector('input[name="date"]').value = '';
            document.querySelector('input[name="q"]').value = '';
            this.closest('form').submit();
        });

        document.getElementById('searchInput').addEventListener('input', function() {
            let query = this.value;
            if (query.length > 2) {
                let suggestions = ['aaaaaaa', 'Camping', 'Zaghouen', 'Public Event'];
                let filteredSuggestions = suggestions.filter(s => s.toLowerCase().includes(query.toLowerCase()));
                let suggestionsHtml = filteredSuggestions.map(s => `<div class="suggestion-item">${s}</div>`).join('');
                document.getElementById('suggestions').innerHTML = suggestionsHtml;
                document.getElementById('suggestions').style.display = 'block';
            } else {
                document.getElementById('suggestions').style.display = 'none';
            }
        });

        document.addEventListener('click', function(event) {
            if (!document.getElementById('searchInput').contains(event.target) && !document.getElementById('suggestions').contains(event.target)) {
                document.getElementById('suggestions').style.display = 'none';
            }
        });
    </script>
</body>
</html>