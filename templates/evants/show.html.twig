{% extends 'base.html.twig' %}

{% block title %}Evants{% endblock %}

{% block body %}
    <h1>Evants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evant.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ evant.date ? evant.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_evants_index') }}">back to list</a>

    <a href="{{ path('app_evants_edit', {'id': evant.id}) }}">edit</a>

    {{ include('evants/_delete_form.html.twig') }}
{% endblock %}
