{% extends 'base.html.twig' %}

{% block title %}Signalement{% endblock %}

{% block body %}
    <h1>Signalement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signalement.id }}</td>
            </tr>
            <tr>
                <th>Raison</th>
                <td>{{ signalement.raison }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ signalement.dateCreation ? signalement.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_signalement_index') }}">back to list</a>

    <a href="{{ path('app_signalement_edit', {'id': signalement.id}) }}">edit</a>

    {{ include('signalement/_delete_form.html.twig') }}
{% endblock %}
