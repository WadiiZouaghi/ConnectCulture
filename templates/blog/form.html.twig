{% extends 'base.html.twig' %}

{% block title %}Blog Form{% endblock %}

{% block body %}
<h1>{{ form.vars.value.id ? 'Edit Blog' : 'Create Blog' }}</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-3">Save</button>
{{ form_end(form) }}

<a href="{{ path('blog_index') }}" class="btn btn-secondary mt-3">Back to list</a>
{% endblock %}
